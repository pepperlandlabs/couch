/**
 * index.js will get the `shrink` instance of the `Shrink` class
 * and this will be exposed. Now, in the test folder, when require('..')
 * is called, it goes to the root folder of the app and searches for
 * index.js. Index.js will module.export the `shrink` object.
 * Now, in the ./test/shrink.js, the `shrink` variable would be the
 * `shrink` object created and exported from the ./lib/shrink.js file.
 */

module.exports = require('./lib/shrink');