doctype html
html(lang="en")
  head
    title= pageTitle
  body
    h3 Active GoInstant Rooms
    ul
      each room in rooms
        li
          a(href='/rooms/#{room.id}') #{room.name}
          | &nbsp; (#{room.users} users, created at: #{moment(room.created_at).format('lll')})
